import { AppShell, Breadcrumbs, PageIntro, ScholarshipCard, SectionHeading } from '@/components/core';
import { scholarships } from '@/lib/data';

export default function ScholarshipsPage() {
  return <AppShell eyebrow="Funding, eligibility, and the fine print"><section className="page-hero"><div className="page-container"><Breadcrumbs items={[{ label: 'Scholarships' }]} /><PageIntro eyebrow="Funding should be researchable" title="Find help with the cost." description="Scholarships are shown with provider, level, deadline, coverage, and an eligibility state. Unknown means you need to verify — not that the answer is yes." /></div></section><div className="page-container page-content"><SectionHeading eyebrow="Funding database" title={`${scholarships.length} starting points`} link="Search scholarships" href="/discover?type=scholarships" /><div className="scholarship-list">{scholarships.map((scholarship) => <ScholarshipCard key={scholarship.id} scholarship={scholarship} />)}</div><div className="editorial-band" style={{ marginTop: 34 }}><div><p className="eyebrow">Eligibility, handled carefully</p><h2>“Unknown” is more useful than a <em>guess.</em></h2><p>Scholarship eligibility can depend on nationality, qualification, program, timing, and the source’s own definitions. We keep those dimensions visible.</p></div><div className="editorial-graphic" style={{ background: 'var(--saffron)', color: 'var(--ink)' }}><span className="graphic-label" style={{ color: 'var(--ink)' }}>Every card includes</span><strong>Provider · support · deadline · source</strong><small style={{ color: 'var(--ink)' }}>Confirm before you apply ↗</small></div></div></div></AppShell>;
}
