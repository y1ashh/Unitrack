import Link from 'next/link';
import { AppShell, Breadcrumbs, EmptyState, PageIntro, ProgramCard, SectionHeading, UniversityCard } from '@/components/core';
import { programs, universities } from '@/lib/data';

export default function SavedPage() {
  return <AppShell eyebrow="Your shortlist, in progress"><section className="page-hero"><div className="page-container"><Breadcrumbs items={[{ label: 'Saved' }]} /><PageIntro eyebrow="Saved workspace" title="Keep the maybe list." description="Save a university or program as you research. These demo records are ready for authentication-backed persistence when a database is connected." /></div></section><div className="page-container page-content"><section><SectionHeading eyebrow="Saved programs" title="Two starting points" link="Compare saved" href="/compare" /><div className="program-grid">{programs.slice(0, 2).map((program) => <ProgramCard key={program.id} program={program} />)}</div></section><section className="content-section"><SectionHeading eyebrow="Saved universities" title="Places to revisit" link="Browse universities" href="/universities" /><div className="university-grid">{universities.slice(0, 2).map((university) => <UniversityCard key={university.id} university={university} />)}</div></section><section className="content-section"><SectionHeading eyebrow="Custom lists" title="Organize the shortlist" /><div className="tracker-grid"><div className="tracker-column"><h3>Canada CS <span>3 items</span></h3><div className="tracker-item"><span className="tracker-marker" /><div><p>Master of Information</p><small>University of Toronto</small></div><time>Saved</time></div><div className="tracker-item"><span className="tracker-marker" /><div><p>New list</p><small>Choose a name that helps</small></div><time>＋</time></div></div><div className="empty-state"><span className="empty-icon">＋</span><h3>Create another list</h3><p>Group a field, a city, a degree, or simply the options you want to talk through.</p><Link className="button button-dark" href="/tracker">Open planning tools</Link></div></div></section></div></AppShell>;
}
